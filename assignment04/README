To run the programs just execute the runme file.

To view the output of program 3 check the results directory.
The pictures are in the pdfs directory. The data files that generated them
are in the data directory.
